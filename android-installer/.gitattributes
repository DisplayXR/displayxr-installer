# The Gradle wrapper's launchers are generated by Gradle and must keep their
# native line endings: gradlew.bat is only ever run by cmd.exe, gradlew only by
# a POSIX shell.
gradlew        text eol=lf
gradlew.bat    text eol=crlf
*.jar          binary
